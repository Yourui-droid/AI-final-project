# AI Final Project

## Project Description
This project is developed as the final assignment for the AI course. It demonstrates an end-to-end AI workflow including data preparation, model execution/training, and result generation.

## Environment Setup
- Python 3.8+
- Install dependencies:
```bash
pip install -r requirements.txt
git clone https://github.com/<your-username>/AI-final-project.git
pip install -r requirements.txt
python main.py
